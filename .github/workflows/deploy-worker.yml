name: Deploy Worker (shv-api)

on:
  push:
    branches: [ "main" ]
    paths:
      - "workers/shv-api/**"
      - ".github/workflows/deploy-worker.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-worker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workers/shv-api

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # (Debug) xem wrangler.toml và marker trong index.js
      - name: Verify source markers
        run: |
          echo "=== wrangler.toml ==="
          cat wrangler.toml
          echo "=== markers in src/index.js ==="
          grep -n "/shipping/areas/ward" src/index.js || true
          grep -n "payload.weight = Number(body.weight_gram" src/index.js || true

      # (Debug) xác thực token/account
      - name: Wrangler whoami
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken:  ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          workingDirectory: workers/shv-api
          command: whoami

      # Publish (in verbose để thấy log build)
      - name: Publish to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken:  ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          workingDirectory: workers/shv-api
          command: deploy --verbose
        env:
          # CHỈ cần nếu bạn KHÔNG set ở Dashboard → Variables
          SHIPPING_API_KEY: ${{ secrets.SHIPPING_API_KEY }}
          SUPER_BASE:       ${{ secrets.SUPER_BASE }}

      # (Debug) liệt kê deployments
      - name: List deployments
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken:  ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          workingDirectory: workers/shv-api
          command: deployments list
