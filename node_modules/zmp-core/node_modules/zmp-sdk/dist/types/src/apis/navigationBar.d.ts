import * as DataType from '../types/inputData';
import { ZaloMiniProgramSDK } from './sdk';
export declare class NavigationBar {
    /**
     * A promise for the user's favorite color.
     *
     * @promise FavoriteColorPromise
     * @fulfill {string} The user's favorite color.
     * @reject {TypeError} The user's favorite color is an invalid type.
     * @reject {MissingColorError} The user has not specified a favorite color.
     */
    /**
     * Dynamically sets the title of the current page.
     *
     * @async
     * @function setNavigationBarTitle
     * @category Navigation Bar
     *
     * @example
     *
     * zmpApis.setNavigationBarTitle({
     *  title: "Current Page"
     * }).then((res) => {
     *  console.log("Success");
     * }).catch((error) => {
     *  console.log("Fail");
     * });
     *
     * @param {Object} object
     * @param {string} object.title - The title of the page
     *
     * @return {Promise}
     *
     */
    static setNavigationBarTitle(this: ZaloMiniProgramSDK, object: DataType.NavBarTitleType): Promise<any>;
    /**
     * Sets the color of the navigation bar in the page.
     *
     * @async
     * @function setNavigationBarColor
     * @category Navigation Bar
     *
     * @example
     * zmpApis.setNavigationBarColor({
     *  color: "#000000"
     * }).then((res) => {
     *  console.log("Success");
     * }).catch((error) => {
     *  console.log("Fail");
     * });
     *
     * @param {Object} object
     * @param {string} object.color - Color value, whose valid value is hexadecimal color
     *
     *
     */
    static setNavigationBarColor(this: ZaloMiniProgramSDK, object: DataType.NavBarColorType): Promise<any>;
    /**
     * Dynamically sets the left button of the current page.
     *
     * @async
     * @function setNavigationBarLeftButton
     * @category Navigation Bar
     *
     * @example
     * zmpApis.setNavigationBarLeftButton({
     *  buttonType: 1,
     *  confirmToExit: 1,
     *  dialogMessage: 'Bạn có muốn đóng app này không?'
     * }).then((res) => {
     *  console.log("Success");
     * }).catch((error) => {
     *  console.log("Fail");
     * });
     *
     * @param {Object} object
     * @param {Number} object.buttonType
     * Left button type:
     *  - 0: Back
     *  - 1: Close
     *  - 2: Both
     * @param {Number} object.confirmToExit
     * Behavior when click close button:
     *  - 0: Close without confirm
     *  - 1: Close with native confirm dialog
     *  - 2: User will handle event close
     * @param {string} [object.dialogMessage="Bạn có muốn thoát?"] - Specifies a message that will be shown on native dialog. Only when ```confirmToExit``` is 1
     * @param {Function} [object.handleCloseEvent] - Specifies a function that will be called when user click close button. Only when ```confirmToExit``` is 2
     */
    static setNavigationBarLeftButton(this: ZaloMiniProgramSDK, object: DataType.NavBarLeftMenuType): Promise<any>;
    /**
     * @ignore
     * Dynamically sets the right button of the current page.
     *
     * @ignore
     * @category Navigation Bar
     *
     * @example
     * zmpApis.setNavigationBarRightButton({
     *  supportToolBar: 1,
     *  reset: 0,
     *  menuListItems: [{
     *    title: "Post on timeline",
     *    action: "zalo.web.internal.postfeed",
     *    showAsAction: 0,
     *    iconUrl: "https://cdn3.iconfinder.com/data/icons/user-interface-3-3/34/251-512.png",
     *    type: 0,
     *    data: {}
     * }],
     * }).then((res) => {
     *  console.log("Success");
     * }).catch((error) => {
     *  console.log("Fail");
     * });
     *
     * @param {Object} object
     * @param {Number} object.title - Title of a menu item
     * @param {Number} object.action - Native action
     * @param {string} object.showAsAction
     * If ```showAsAction``` is 1 and ```iconUrl``` can load,
     * this item will be shown outside and next to more-button. Maximum 2 item will be shown
     * @param {Function} object.iconUrl - Specifies icon of a menu item
     * @param {0} object.type
     * @param {Object} object.data - Data depends on ```action```
     */
    static setNavigationBarRightButton(this: ZaloMiniProgramSDK, object: DataType.NavBarRightMenuType): Promise<any>;
}
