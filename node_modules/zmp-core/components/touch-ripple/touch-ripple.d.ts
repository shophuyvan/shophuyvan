import { zmpDOMArray } from 'zmp-dom';
import ZMP, { CSSSelector, ZMPEventsClass, ZMPPlugin } from '../app/app-class';

export namespace TouchRipple {
  interface TouchRipple {
    $rippleWaveEl: zmpDOMArray;
    rippleTransform: string;
    removing: boolean;
    remove(): void;
  }
  interface AppMethods {
    touchRipple: {
      create($el: zmpDOMArray, x: number, y: number): TouchRipple;
    };
  }
  interface AppParams {}
  interface AppEvents {}
}

declare const TouchRippleComponent: ZMPPlugin;
export default TouchRippleComponent;
