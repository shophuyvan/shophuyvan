import { Support } from '../../shared/get-support';
import ZMP, { ZMPPlugin } from '../../components/app/app-class';

export namespace Support {
  interface AppMethods {
    /** Object with properties about supported features */
    support: Support;
  }
  interface AppParams {}
  interface AppEvents {}
}

declare const SupportModule: ZMPPlugin;

export default SupportModule;
