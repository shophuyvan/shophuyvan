import { useState } from 'react';
import { zmp, zmpready, theme } from './zmp';
export var useTheme = function useTheme() {
  var _useState = useState(zmp ? theme : null),
      t = _useState[0],
      setTheme = _useState[1];

  if (!zmp) {
    zmpready(function () {
      setTheme(theme);
    });
  }

  return t;
};