import React from 'react';
import Icon from './icon';
import { ICON_SIZE_SM, ICON_CHECK, ICON_INFO_CIRCLE } from '../../common/constants';

var ZMPInputInfo = function ZMPInputInfo(_ref) {
  var info = _ref.info,
      valid = _ref.valid;

  if (info) {
    return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Icon, {
      size: ICON_SIZE_SM,
      className: "item-input-info-icon",
      zmp: valid ? ICON_CHECK : ICON_INFO_CIRCLE
    }), info);
  }

  return null;
};

export default ZMPInputInfo;