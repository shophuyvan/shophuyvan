{
  "name": "shv-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "format": "prettier -w \"**/*.{html,css,js,json,md,yml,yaml}\"",
    "lint": "pnpm -r run lint || true"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  },
  "packageManager": "pnpm@9.9.0"
}