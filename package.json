{
  "private": true,
  "type": "module",
  "scripts": {
    "fmt": "prettier --write \"**/*.{js,css,html,json,yml,yaml,md}\"",
    "lint": "eslint \"**/*.js\"",
    "syntax": "node --check **/*.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ci:verify": "npm run fmt && npm run lint && npm run syntax && npm run typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "globals": "^15.8.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "name": "shv-api"
}