export function formatPrice(v){return (v||0).toLocaleString('vi-VN')+'đ'} export function pickPrice(product,variant){if(variant){const base=(variant.sale_price??variant.price)??0;const original=variant.sale_price?variant.price:0;return {base,original}}const base=(product.sale_price??product.price)??0;const original=product.sale_price?product.price:0;return {base,original}}